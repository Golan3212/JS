'use strict'
/*Оператор *= дает команду переменной умножить на 2 и присвоить переменноа а результат(4). Затем вычисляется переменная х (1+4). х=5, а=4  */
let z = 2;
let x = 1 + (z *= 2);
console.log(x);